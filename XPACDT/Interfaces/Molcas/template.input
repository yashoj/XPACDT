&SEWARD
    basis = {basis}     * Basis set to use for the computation
    group = nosym       * Symmetry to be considered
    coord               * Coordinates of the atoms
        {n_atoms}
                        * Mandatory ignored line for XYZ format
        {cartesian_coo}

&SCF  * SCF computation assuming 0 total charge

{rasscf}

&ALASKA  * Run Alaska to get the gradient
    pnew

* We only compute the lower subdiagonal triangle of the NAC vector matrix
* since it is antisymmetric (for real vectors). That means
* d_IJ = -d_JI
* and
* d_II = 0
>> FOREACH I in (1 .. {n_states})
    >> EVAL JMAX = $I - 1
    >> FOREACH J in (1 .. JMAX})

        &ALASKA  * Run Alaska to get the nonadiabatic coupling vectors (NAC)
            nocsf
            nac = $I $J

    >> ENDDO
>> ENDDO